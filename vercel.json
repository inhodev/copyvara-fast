{
    "version": 2,
    "outputDirectory": "frontend-minimal/dist",
    "builds": [
        {
            "src": "api/*.ts",
            "use": "@vercel/node"
        },
        {
            "src": "frontend-minimal/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        },
        {
            "source": "/(.*)",
            "destination": "/frontend-minimal/$1"
        }
    ]
}